{
  "name": "auto-window-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Full stack dashboard for auto window (Render deploy)",
  "license": "MIT",
  "scripts": {
    "prebuild": "node scripts/gen-config.cjs",
    "build": "echo Build complete",
    "start": "node server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mqtt": "^5.10.1"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}
